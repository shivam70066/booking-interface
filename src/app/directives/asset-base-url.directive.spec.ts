import { AssetBaseUrlDirective } from './asset-base-url.directive';

describe('AssetBaseUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new AssetBaseUrlDirective();
    expect(directive).toBeTruthy();
  });
});
