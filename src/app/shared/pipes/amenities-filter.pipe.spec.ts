import { AmenitiesFilterPipe } from './amenities-filter.pipe';

describe('AmenitiesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AmenitiesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
